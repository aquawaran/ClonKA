# 🚀 Настройка Clone с Supabase

## 📋 План действий

1. **Создать Supabase проект**
2. **Настроить базу данных**
3. **Обновить конфигурацию**
4. **Деплой на Vercel**

---

## 🗄️ Шаг 1: Создание Supabase проекта

1. Перейдите на [supabase.com](https://supabase.com)
2. Войдите через GitHub
3. Нажмите "New Project"
4. Выберите организацию или создайте новую
5. Укажите:
   - **Project name**: `clone-social`
   - **Database Password**: создайте надежный пароль
   - **Region**: выберите ближайший регион
6. Нажмите "Create new project"

---

## 🔧 Шаг 2: Настройка базы данных

1. Откройте **SQL Editor** в Supabase
2. Скопируйте и вставьте содержимое файла `supabase-schema.sql`
3. Нажмите "Run" для выполнения SQL
4. Проверьте что все таблицы созданы в **Table Editor**

---

## 📁 Шаг 3: Настройка Storage

1. Перейдите в **Storage**
2. Создайте два бакета:
   - `avatars` - для аватаров пользователей
   - `posts-media` - для медиа постов
3. Настройте **Policies** для каждого бакета:
   ```sql
   -- Для avatars
   CREATE POLICY "Users can upload own avatar" ON storage.objects
   FOR INSERT WITH CHECK (bucket_id = 'avatars' AND auth.uid()::text = (storage.foldername(name))[1]);

   CREATE POLICY "Anyone can view avatars" ON storage.objects
   FOR SELECT USING (bucket_id = 'avatars');

   -- Для posts-media
   CREATE POLICY "Users can upload post media" ON storage.objects
   FOR INSERT WITH CHECK (bucket_id = 'posts-media' AND auth.uid()::text = (storage.foldername(name))[1]);

   CREATE POLICY "Anyone can view post media" ON storage.objects
   FOR SELECT USING (bucket_id = 'posts-media');
   ```

---

## ⚙️ Шаг 4: Обновление конфигурации

1. В Supabase перейдите в **Project Settings** → **API**
2. Скопируйте:
   - **Project URL** (например: `https://abc123.supabase.co`)
   - **anon public** key

3. Откройте файл `supabase-config.js`
4. Замените:
   ```javascript
   const SUPABASE_URL = 'https://YOUR_PROJECT_ID.supabase.co';
   const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY';
   ```
   На ваши реальные значения.

---

## 🌐 Шаг 5: Деплой на Vercel

### Вариант А: Через GitHub (рекомендуется)

1. Создайте GitHub репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit - Supabase version"
   git remote add origin https://github.com/yourusername/clone-social.git
   git push -u origin main
   ```

2. Перейдите на [vercel.com](https://vercel.com)
3. Войдите через GitHub
4. Нажмите "New Project"
5. Выберите ваш репозиторий
6. Настройки:
   - **Framework Preset**: Other
   - **Build Command**: оставьте пустым
   - **Output Directory**: `.` (текущая папка)
   - **Root Directory**: оставьте пустым

7. Нажмите "Deploy"

### Вариант Б: Прямая загрузка

1. Перейдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите "Add New..." → "Project"
4. Перетащите папку проекта в браузер
5. Следуйте инструкциям

---

## 🔄 Шаг 6: Настройка переменных окружения

В Vercel добавьте переменные окружения:

1. Перейдите в **Settings** → **Environment Variables**
2. Добавьте:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

---

## ✅ Шаг 7: Тестирование

1. Откройте ваш сайт на Vercel
2. Проверьте:
   - Регистрация новых пользователей
   - Вход в систему
   - Создание постов
   - Загрузка изображений
   - Реакции и комментарии

---

## 🔍 Проверка работы

### База данных
- В Supabase **Table Editor** должны появляться записи пользователей и постов
- В **Storage** должны загружаться файлы

### Real-time
- Откройте сайт в двух вкладках
- Создайте пост в одной вкладке - он должен появиться в другой

---

## 🚨 Возможные проблемы

### 1. CORS ошибка
Убедитесь что в Supabase **Settings** → **API** в CORS origins указано:
```
http://localhost:3000
https://your-vercel-domain.vercel.app
```

### 2. Ошибка загрузки файлов
Проверьте что Storage policies настроены правильно

### 3. Real-time не работает
Убедитесь что RLS policies разрешают подписки

---

## 📱 Мобильная версия

Сайт автоматически адаптируется под мобильные устройства. Проверьте на телефоне.

---

## 🎯 Результат

После настройки у вас будет:
- ✅ Социальная сеть работающая 24/7
- ✅ Все данные сохраняются в PostgreSQL
- ✅ Медиа файлы хранятся в Supabase Storage
- ✅ Real-time обновления
- ✅ Бесплатный хостинг на Vercel
- ✅ SSL сертификат
- ✅ CDN для быстрой загрузки

---

## 🔄 Резервное копирование

Для безопасности регулярно делайте бэкапы:
1. В Supabase **Settings** → **Database** → **Backups**
2. Или экспортируйте данные через **SQL Editor**

---

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте консоль браузера
2. Посмотрите логи в Supabase
3. Проверьте настройки Vercel

Готово! 🎉
